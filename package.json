{
  "name": "typescript-playground",
  "version": "1.0.0",
  "description": "Playground to experiment and test some behaviors of TypeScript",
  "author": "Maud Gautier",
  "main": "src/index.ts",
  "scripts": {
    "test": "jest --watch --coverage",
    "tsc": "tsc --noEmit",
    "prettier": "prettier --loglevel silent ./ --check --write"
  },
  "type": "module",
  "devDependencies": {
    "@types/jest": "^29.5.1",
    "jest": "^29.5.0",
    "prettier": "^2.8.8",
    "ts-jest": "^29.1.0",
    "typescript": "^5.0.4"
  },
  "prettier": {
    "printWidth": 120
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com:MaudGautier/typescript-playground.git"
  }
}
